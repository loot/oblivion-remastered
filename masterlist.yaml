# LOOT ignores anything that doesn't come under 'prelude', 'bash_tags',
# 'globals', 'groups', or 'plugins', so a section like 'common' is useful for
# storing data that gets re-used.
prelude:

common:

bash_tags:

globals:

groups:

plugins:
